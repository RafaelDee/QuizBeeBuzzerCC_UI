import { LoadingDirective } from './acync.directive';

describe('LoadingDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadingDirective();
    expect(directive).toBeTruthy();
  });
});
